# Project-scoped Codex collaboration defaults for UPR.
# The primary model/effort is selected by the user; mobile program sessions use GPT-5.6 Sol Ultra.

[agents]
enabled = true
max_concurrent_threads_per_session = 3
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "medium"
