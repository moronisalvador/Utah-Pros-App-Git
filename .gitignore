# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.dev.vars
.dev.vars.*
.wrangler/
test-results/
playwright-report/
playwright/.auth/

# Claude Code worktrees
.claude/worktrees/
# Machine-local Claude permissions must remain untracked after the CAP-SEC-001 owner cleanup.
.claude/settings.local.json
# Per-session instruction-load evidence. Machine-local and append-only; the
# report is derived, never the log itself.
.claude/logs/
# Per-session worktree/dirty-state diary (worktree-lifecycle.md). Describes
# THIS machine's sessions, so it is local state and never repository history.
.claude/session-ledger.json
CLEANUP-TEST-DATA.sql
.env

# Generated local-only DB seed bundle (scripts/db-local-bootstrap.mjs)
supabase/seeds/*.generated.sql
