{
  "name": "upr-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "UPR's private remote MCP server: QuickBooks Online + the UPR (Supabase) database. OAuth-locked to a single owner email.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "build-index": "node scripts/build-index.js"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.5"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
