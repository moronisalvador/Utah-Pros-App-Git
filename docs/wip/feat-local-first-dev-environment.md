---
branch: feat/local-first-dev-environment
ships: true
opened: 2026-08-15
---

# What

fix(native): allowlist composerAttachmentStore so the iOS bundle builds

# Why it matters

(not stated — say why this matters before it is forgotten)

# Next action

Wire assertProviderCallAllowed into CallRail + Encircle outbound helpers (needs env threaded into resolveCallRecording and friends), then build response fixtures so the messaging path can actually be exercised offline rather than only blocked.
