# Domain: Documents & Storage

Job files (photos, notes, PDFs, generated reports), the customer-facing e-signature pipeline (sign_requests + token-gated public RPCs + SignPage/submit-esign worker), editable legal document templates, and the two Storage buckets (`job-files` public, `message-attachments` private/worker-only). The domain is heavily used and functionally healthy — 3 of 4 tables and all 12 RPCs are live, job_documents alone shows 72k seq scans — but its authorization layer is the weakest in the audit set: every live policy on the public-schema tables is either always-true or role-exclusion-only, the TO-public CRM-partner policy family silently re-opens **anonymous full CRUD on job_documents**, the public bucket exposes signed legal PDFs on unauthenticated URLs, and the DB never enforces e-sign status/expiry on retrieval (client/worker band-aid, audit-documented). One table (document_requests), two columns, and one policy are dead.

## Tables (4)

### job_documents — USED
- Purpose: one row per file/photo/note attached to a job (uploaded, generated, imported, or produced by signing).
- Evidence: 25+ live call sites. Reads: `src/pages/JobPage.jsx:78,838`, `src/pages/ClaimPage.jsx:124`, `src/pages/tech/TechJobDetail.jsx:214`, `TechClaimDetail.jsx:268`, `TechJobAlbum.jsx:69`, `TechClaimAlbum.jsx:91`, `TechAppointment.jsx:143`, `TechRoomDetail.jsx:99`, `src/pages/tech/v2/hub/PhotosNotes.jsx:109`, `src/pages/InvoiceEditor.jsx:501`, `src/components/tech/GenerateReportButton.jsx:53`, `MergeModal.jsx:167`. Writes: `insert_job_document` RPC from 15 call sites (usePhotoUpload.js:107, photoDispatcher.js:135, noteDispatcher.js:59, TimeTracker.jsx:357, tech pages, workers demo-sheet-pdf.js:90, generate-water-loss-report.js:98, google-drive-import.js:73), direct `db.insert` JobPage.jsx:870, caption `db.update` (TechAppointment.jsx:279, HubDock.jsx:143, PhotosNotes.jsx:140, PhotoCaptureButton.jsx:149), deletes JobPage.jsx:696,874. Cross-domain function reads: get_customer_detail, get_tech_dashboard, get_tech_claims, get_job_rooms, get_claim_rooms, get_water_loss_report_data, get_demo_sheet_pdf_gaps, delete_room, merge_jobs.
- Prod stats: 79 live rows, ins 112 / upd 6 / del 40, seq 72,607 / idx 3,898, last scans 2026-07-29.
- Provenance: untracked — predates schema-as-code (no CREATE TABLE in supabase/migrations/).
- Columns: 16 total — 13 used, 1 uncertain, 1 dead, 0 band-aid, 1 duplicated.
  - `encircle_media_id` — DEAD. Zero hits for `encircle_media_id` (patterns `'N'`, `"N"`, `\bN\b`) across src/, functions/, scripts/, upr-mcp/, tests/, supabase/tests/; zero hits in fndefs (only the partial unique index `job_documents_encircle_media_id_idx` exists); none of the 8 Encircle workers (`functions/api/encircle-*.js`, `sync-encircle.js`, `sync-claim-to-encircle.js`) touches job_documents at all. No writer, no reader. Column-level prod stats don't exist; table stats can't clear it, but the code trail is empty.
  - `esign_status` — DUPLICATED. Written once by `complete_sign_request` (`'signed'`); mirrors `sign_requests.status` for the row linked via `sign_request_id`; zero readers anywhere in code or fndefs.
  - `updated_at` — uncertain. Set explicitly at desktop insert (JobPage.jsx:869) and by complete_sign_request, but no trigger maintains it on UPDATE (caption edits leave it stale); no reader found; carried by `select=*`.
  - `sign_request_id` — used (write-only lineage link): written by complete_sign_request, FK + index exist, no code reader (JobPage cleanup matches on file_path instead).
  - `file_size` — used, but only the desktop JobPage insert sets it; the `insert_job_document` RPC has no file_size param, so all tech/worker uploads leave it NULL (display sites JobPage.jsx:912, ClaimPage.jsx:809, CustomerPage.jsx:501 tolerate null).
- Policies (8 — all classified under Policies logic below): 4× `anon_*` (TO authenticated, always-true) BAND-AID + 4× `job_documents_*` (TO public, `NOT is_crm_partner(auth.uid())`) BAND-AID — the public-role family passes for anon (`is_crm_partner(NULL)` = false) and anon retains full table grants, so anon effectively has CRUD (see Structural problems #1). The two families are also mutually redundant for authenticated users (multiple permissive policies per cmd).
- Triggers: `trg_document_events` (AFTER INSERT OR DELETE → trigger_document_events → log_system_event) — USED.
- Notes: FKs → jobs (CASCADE), appointments (SET NULL), rooms (SET NULL), sign_requests (SET NULL), employees. File bytes live in the `job-files` bucket; `file_path` sometimes carries the `job-files/` prefix and sometimes not (normalized by `stripBucketPrefix`, mediaCompress.js:105 — a disclosed legacy inconsistency).

### sign_requests — USED
- Purpose: one row per e-signature request (token, doc type, signer, status lifecycle pending→signed/cancelled, open-tracking, recon-agreement consents).
- Evidence: created via `create_sign_request` (send-esign.js:149); completed via `complete_sign_request` (submit-esign.js, incl. the msg-domain wrapper `complete_sign_request_with_work_authorization_sms_consent` which calls it); read by SignPage.jsx:196 (anon, token RPC), JobPage.jsx:838, TechJobDocuments.jsx:96, TechJobDetail.jsx:215, TechAppointment.jsx:148; updated (cancel) JobPage.jsx:717, TechJobDocuments.jsx:191; worker resend-esign.js:65,105; track-open.js → `record_email_open`. Function reads: get_job_hub, get_contact_activity, get_commissions, get_dashboard_action_items, get_real_job_evidence_mismatches (daily cron jobid 10), merge_jobs/merge_contacts. FK from `work_authorization_sms_consents` (msg domain).
- Prod stats: 46 live rows, ins 118 / upd 137 / del 124, seq 782 / idx 16,371, last scans 2026-07-29.
- Provenance: untracked — predates schema-as-code (consent/tracking columns added later; adding migrations not located).
- Columns: 25 total — 23 used, 1 uncertain, 1 dead, 0 band-aid, 0 duplicated.
  - `link_clicked_at` — DEAD. Zero hits (`'N'`/`"N"`/`\bN\b`) across src/, functions/, scripts/, upr-mcp/, tests/, supabase/tests/; zero hits in fndefs (record_email_open writes only email_opened_at/email_open_count; complete_sign_request never touches it); resend-esign.js resets only the open pair. The click half of e-sign tracking never shipped.
  - `created_at` — uncertain (generic, default-only, carried by `select=*`; ordering uses sent_at instead).
  - `expires_at` — used, but enforced only in SignPage.jsx:201 and submit-esign.js:173 — never in the DB (see Structural problems #3).
  - `email_opened_at`/`email_open_count` — used (written by record_email_open via the public tracking pixel, reset by resend-esign, displayed JobPage.jsx:746-747).
  - `consent_terms/commitment/esign/authority`, `consents_signed_at` — used (recon-agreement flow; complete_sign_request + audit payload).
- Policies (4): `anon_insert/select/update_sign_requests` (TO authenticated, always-true) BAND-AID ×3 — fully subsumed by `authenticated_all_sign_requests` (ALL TO authenticated, always-true) BAND-AID. Net: any authenticated user can read/cancel/rewrite any sign request; RLS carries no authorization.
- Triggers: `sign_requests_updated_at` (BEFORE UPDATE → update_sign_requests_updated_at) USED; `signreq_real_job` (AFTER INSERT/UPDATE OF status → trg_signreq_real_job → mark_job_real on signed work_auth/recon) USED; `crm_sign_request_signed_advance` (AFTER INSERT/UPDATE OF status → crm_trg_sign_request_signed, CRM-owned fn → auto-advance lead to Won) USED — exercised by supabase/tests/crm_pipeline_auto_advance.test.js (db lane).
- Notes: token is the security capability (unique index sign_requests_token_key). FKs → jobs (CASCADE), contacts (SET NULL), employees (SET NULL). The signing token flow is the §2 public allowlist "public e-sign pages" entry.

### document_templates — USED
- Purpose: editable legal text sections (work_auth, direction_pay, change_order, recon_agreement — per division) rendered into e-sign pages and signed PDFs.
- Evidence: read via `get_document_templates` (Templates.jsx:47, TemplatesEditor.jsx:63), token-scoped anon read via `get_sign_document_templates` (SignPage.jsx:211), direct worker read submit-esign.js:196 (service role, builds the signed PDF), written via `upsert_document_template` (TemplateEditor.jsx:80). Consumed by ReconAgreementContent.jsx.
- Prod stats: 24 live rows, ins 24 / upd 14 / del 0, seq 127 / idx 62, last scans 2026-07-28.
- Provenance: untracked — predates schema-as-code. The anon-read closure sequence is tracked: 20260708_dbf_p3_document_templates_anon_bridge.sql → 20260708_dbf_p3_sign_document_templates_rpc.sql → 20260708_dbf_p3_drop_document_templates_bridge.sql.
- Columns: 8 total — 7 used, 1 uncertain, 0 dead, 0 band-aid, 0 duplicated.
  - `created_at` — uncertain (generic, default-only, carried by SETOF; no explicit reader found).
  - `updated_at` — used (explicitly maintained by upsert_document_template).
- Policies (2): `anon read document_templates` (SELECT TO authenticated, always-true) BAND-AID — misleading legacy name, and fully subsumed by `anon write document_templates` (ALL TO authenticated, always-true) BAND-AID. Net: any authenticated user can rewrite legal contract text (see Structural problems #4).
- Triggers: none.
- Notes: distinct from the sched-domain `template_phases`/`template_tasks`/`template_dependencies` family (schedule templates) and from `message_templates` (msg domain) — verified, not conflated; no FK or code path links them.

### document_requests — DEAD
- Purpose (intended): track documents requested FROM a customer (document_type, requested_at, received_at, media_url) — a request/receive workflow that never shipped.
- Evidence trail (dead claim): Grep patterns `'document_requests'`, `"document_requests"`, `\bdocument_requests\b` across src/, functions/, scripts/, upr-mcp/, tests/, supabase/tests/, .github/ (js, jsx, mjs, sql, toml, yml) → 1 hit: `upr-mcp/src/codeIndex.js:6223`, a generated search-token metadata entry, not a call site. fndefs: referenced only by `merge_jobs` (fndefs-q3-06.sql:442, `UPDATE document_requests SET job_id=... WHERE job_id=...` — blanket child re-point that moves rows which can never exist). triggers.json: none. views.json: none. prod-cron.json: none. publications.json: not in supabase_realtime. Not in DevTools TABLE_LIST. Prod stats corroborate: **0 live rows, ins 0 / upd 0 / del 0**, seq 78 (catalog-style scans, last 2026-07-21), idx 0 — with the standard stats-since-reset caveat. Verdict: dead-by-chain — reached only via merge_jobs housekeeping over an always-empty table.
- Provenance: untracked — predates schema-as-code.
- Columns: 8 total — 8 dead (dead with table): id, job_id, document_type, requested_at, received_at, media_url, requested_by, notes.
- Policies: `allow_authenticated_document_requests` (ALL, authenticated, true/true) — DEAD (policy on a dead table).
- Triggers: none.
- Notes: FKs → jobs, employees. Drop candidate (table + policy + 2 FKs + pkey) in a reviewed destructive change.

## Storage buckets & storage-schema objects

- **job-files** (`public: true`) — USED, heavily. Uploads: usePhotoUpload.js:44 (BUCKET='job-files'), photoDispatcher.js, FeedbackAttachments.jsx:164, JobPage desktop upload; workers demo-sheet-pdf.js:85 (`db.uploadStorage('job-files', ...)`), generate-water-loss-report.js:79, google-drive-import.js:62, qbo-attach.js. Reads: public URLs everywhere (`/storage/v1/object/public/job-files/...` + `render/image` thumbnails via thumbUrl), analyze-xactimate.js:165, GenerateReportButton. Deletes: JobPage.jsx:874, FeedbackAttachments.jsx:250. Bucket posture: PUBLIC — every object readable/listable without auth; signed legal PDFs (`signed_file_path`) live here. P8 (private + signed URLs) is authored intent, unshipped.
- **message-attachments** (`public: false`) — USED via privileged path only (by design, per 20260708_dbf_p2_storage_lockdown.sql): zero object policies; workers use service role + `signStorage` (callrail-mms.js:18 CALLRAIL_MMS_BUCKET, message-media.js:9 MESSAGE_MEDIA_BUCKET, messaging-transport.js:61). Cross-domain (msg) — noted, not classified beyond the bucket/policy posture I own. Note the P2 migration called it dead at the time; the CallRail MMS work (2026-07-24) revived it worker-side without adding browser policies — correct least-privilege.
- Storage policies (4, all on storage.objects):
  - `job_files_select` (SELECT TO public, `bucket_id='job-files'`) — BAND-AID: broad public read incl. anonymous listing of the whole bucket; kept deliberately (database-standard §2 "public job-file READ (temporary)") until P8 signed URLs; it is also what carries authenticated listing today.
  - `anon_read_job_files` (SELECT TO anon, same predicate) — DUPLICATED: fully subsumed by job_files_select (anon ⊂ public, same cmd + predicate); removing it changes nothing.
  - `job_files_authenticated_insert` (INSERT TO authenticated, bucket check) — USED: the deliberate P2 least-privilege floor; no path/owner predicate (audit-documented gap).
  - `job_files_authenticated_delete` (DELETE TO authenticated, bucket check) — USED: same; any staff session can delete any object in the bucket, including signed contract PDFs (Structural problems #2). No UPDATE policy exists for any role (upsert/overwrite via browser fails — consistent with current code).
- Storage triggers (4): `storage.buckets:enforce_bucket_name_length_trigger`, `storage.buckets:protect_buckets_delete`, `storage.objects:protect_objects_delete`, `storage.objects:update_objects_updated_at` — all USED (Supabase-platform-owned integrity triggers; not ours to modify).

## RPCs (12)

| function(args) | class | security | callers / evidence | notes |
|---|---|---|---|---|
| complete_sign_request(p_token, p_signer_name, p_signer_ip, p_signed_file_path, 4× p_consent_*) | used | DEFINER, pinned search_path; EXECUTE: authenticated, service_role | submit-esign.js:97 (fallback) + wrapped by msg-domain `complete_sign_request_with_work_authorization_sms_consent` (primary path, per tests/qa/unit/work-authorization-sms-consent-migration.test.js:123) | Token compare-and-set (`WHERE token=... AND status='pending'`) is the only gate — **no expires_at check**, no caller check; inserts job_documents + system_events audit. Any authenticated user can call it directly and complete an expired-but-pending request. |
| create_sign_request(p_job_id, p_contact_id, p_doc_type, p_signer_name, p_signer_email, p_sent_by, p_divisions) | used | DEFINER, pinned; authenticated, service_role | send-esign.js:149 (worker, behind requireUser) | No caller/role check — any authenticated session can mint signing tokens for any job. p_sent_by is caller-supplied, unverified. |
| get_document_templates() | used | DEFINER, pinned; authenticated, service_role | Templates.jsx:47, TemplatesEditor.jsx:63 | Plain `SELECT *`; DEFINER unnecessary given the table's own (broad) policies; volatility 'v' though read-only. |
| get_sign_document_templates(p_token) | used | DEFINER, STABLE, pinned; **anon**, authenticated, service_role | SignPage.jsx:211 (public signing page) | Deliberate §2 allowlist member (born 20260708_dbf_p3_sign_document_templates_rpc.sql, replacing whole-table anon read). Token-scoped, but no status/expiry predicate — returns template text for signed/expired tokens too (template text is low-sensitivity). |
| get_sign_request_by_token(p_token) | used | DEFINER, pinned; **PUBLIC (bare `=X` acl), anon**, authenticated, service_role | SignPage.jsx:196, submit-esign.js:169; probed by supabase/tests/db_foundation_p3_anon_closure.test.js + anon_closure_tranche_b.test.js (db lane) | **Token-only lookup, no status/expires_at predicate** — returns full job PII (insured_name, address, claim_number, policy_number, adjuster) for any valid token forever; enforcement happens after retrieval (SignPage.jsx:199-201, submit-esign.js:171-173). Audit-documented band-aid boundary. Bare PUBLIC EXECUTE never revoked (managed-Supabase re-grant trap). |
| insert_job_document(p_job_id, p_name, p_file_path, p_mime_type, p_category, p_uploaded_by, p_appointment_id, p_description, p_room_id) | used | DEFINER, pinned; authenticated, service_role | 15 call sites: usePhotoUpload.js:107, photoDispatcher.js:135, noteDispatcher.js:59, TechJobDetail.jsx:260,320, TechAppointment.jsx:205,455, TechClaimDetail.jsx:333,418, TechJobAlbum.jsx:113, TechClaimAlbum.jsx:138, TechRoomDetail.jsx:161, PhotoCaptureButton.jsx:99, HubDock.jsx:107, PhotosNotes.jsx:160, TimeTracker.jsx:357, InvoiceEditor.jsx:514; workers demo-sheet-pdf.js:90, generate-water-loss-report.js:98, google-drive-import.js:73 | No caller check; being DEFINER it bypasses the is_crm_partner exclusion policies (a crm_partner session blocked by RLS could still insert via RPC). p_uploaded_by caller-supplied. No file_size param (tech uploads lose size metadata). |
| move_photo_to_room(p_document_id, p_room_id) | used | DEFINER, pinned; authenticated, service_role | TechAppointment.jsx:285, PhotoCaptureButton.jsx:154, HubDock.jsx:148, PhotosNotes.jsx:145 | No caller check and no job-consistency check — can point a document at any room of any job. |
| record_email_open(p_token) | used | DEFINER, pinned; authenticated, service_role (not anon) | track-open.js:35 (public pixel worker, service-role key) | Token + `status='pending'` gated; monotonic first-open + count. Public reachability is via the worker, not a DB grant — correct. |
| trg_signreq_real_job() | used | DEFINER, pinned (search_path 'public'); trigger fn | Fired by `signreq_real_job` on sign_requests (provenance 20260627_real_job_classification.sql); calls jobs-domain mark_job_real on signed work_auth/recon_agreement | Feeds the real-job evidence reconciler cron (jobid 10) chain. |
| trigger_document_events() | used | DEFINER, pinned; trigger fn | Fired by `trg_document_events` on job_documents; calls log_system_event → system_events | Write-only audit feed (document.uploaded / document.deleted). |
| update_sign_requests_updated_at() | used | INVOKER, pinned; trigger fn | Fired by `sign_requests_updated_at` BEFORE UPDATE | Standard updated_at stamp. Only invoker-mode function in the domain. |
| upsert_document_template(p_doc_type, p_division, p_heading, p_body, p_sort_order) | used | DEFINER, pinned; authenticated, service_role | TemplateEditor.jsx:80 (settings UI) | No role check — any authenticated employee (any role) can rewrite the legal text that gets baked into signed contracts. |

## Structural problems (ranked, worst first)

1. **Anonymous full CRUD on job_documents via the TO-public CRM-partner policies — sev 5.** `job_documents_select/insert/update/delete` (from 20260701_crm_partner_rls_non_crm_tables.sql) are `TO public` with predicate `NOT is_crm_partner(auth.uid())`; `is_crm_partner(NULL)` returns false (plain EXISTS), so the predicate passes for the anon role, and grants.json shows anon retains full table privileges. The P3 anon-policy closure re-scoped only the `anon_*`-named family to authenticated and left this family in place, silently neutralizing the closure for this table: a browser with only the publishable key can select, insert, update, and delete job_documents rows. This is table CRUD, well beyond the §2 allowlist's "public job-file READ" intent. Fix shape: add `TO authenticated` to the four policies (or fold the exclusion into the authenticated family).
2. **job-files bucket: public objects + anonymous listing + unscoped staff delete — sev 4.** Bucket `public: true` (prod-buckets.json) means every object — including signed legal PDFs written by complete_sign_request — is fetchable unauthenticated; `job_files_select` (TO public) + redundant `anon_read_job_files` additionally permit anonymous *listing* of the entire bucket via the Storage API; `job_files_authenticated_delete` lets any staff session delete any object (a signed contract's evidentiary PDF is deletable by any employee, and JobPage's cancel flow already deletes signed files). Deliberately deferred to P8 (private bucket + signed URLs, per 20260708_dbf_p2_storage_lockdown.sql), but P8 is unshipped and the exposure is live.
3. **E-sign enforcement lives above the database — sev 4.** `get_sign_request_by_token` (granted to anon AND bare PUBLIC) does token-only lookup with no status/expires_at predicate and returns job PII; expiry/status are enforced only in SignPage.jsx:199-201 (client) and submit-esign.js:171-173 (worker). `complete_sign_request` checks `status='pending'` but not `expires_at`, and is executable by any authenticated session — an internal caller can complete an expired request or fetch any historical signed request's PII given its token. The audit already documents this band-aid boundary; the DB-side fix (predicate in the RPCs + revoke PUBLIC) is small and contract-compatible.
4. **Authorization theater on the domain's public-schema tables — sev 3.** Every live policy is always-true (`sign_requests` ×4, `document_templates` ×2, `job_documents` anon_* ×4) or exclusion-only (#1); duplicate permissive generations coexist on both job_documents (2 families) and sign_requests (3 single-cmd policies subsumed by an ALL policy), and names lie (`anon_*` policies scoped TO authenticated). Combined with no-caller-check DEFINER RPCs, any authenticated user of any role can cancel/rewrite sign requests and edit legal template text (`upsert_document_template`). Consolidate to one honest policy set per table and role-gate template writes.
5. **document_requests is a dead table — sev 2.** Never wired to any UI/worker/RPC; 0 rows, 0 writes ever recorded; only merge_jobs housekeeping references it. Candidate for a reviewed destructive drop (table + policy + FKs), or for the owner to revive the request-from-customer workflow deliberately.
6. **Dead/orphan columns from half-shipped features — sev 1.** `job_documents.encircle_media_id` (Encircle media import never linked to job Files; carries a unique index that costs writes), `sign_requests.link_clicked_at` (click-tracking half never shipped; open-tracking half did), `job_documents.esign_status` (write-only mirror of sign_requests.status, never read). All droppable in a reviewed change; none is load-bearing.

## Open questions for the owner

1. **Document requests:** was "request a document from the customer and track when it arrives" (document_requests) a workflow you still want? It was built empty and never wired — drop it, or should it be finished?
2. **Encircle media:** should Encircle claim photos ever import into a job's Files tab (that's what `job_documents.encircle_media_id` was for)? The Encircle workers exist but never link media to job documents — is that integration still planned?
3. **E-sign click tracking:** email open tracking shipped; link click tracking (`link_clicked_at`) never did. Do you want click tracking, or should the column go?
4. **Template editing authority:** today any logged-in employee (any role) can rewrite the legal text of Work Authorizations / Reconstruction Agreements via Settings → Templates. Who should actually be allowed to?
5. **Public signed contracts:** signed legal PDFs currently sit on public URLs (anyone with the link, no login, forever) until the signed-URL migration (P8) ships. Is that acceptable short-term, and what priority should P8 get?
6. **Sign-request lifetime:** links expire at 30 days by convention, but the database itself never refuses an expired token — only the signing page and worker do. Any business reason a staff member should be able to complete an expired request, or should the database enforce expiry?

## Search appendix

- Repo greps (scope: src/, functions/, scripts/, upr-mcp/, tests/, supabase/tests/, .github/; globs *.js *.jsx *.mjs *.sql *.toml *.yml): bare-word patterns for each table (`document_requests`, `document_templates`, `job_documents`, `sign_requests`) and each RPC name (`complete_sign_request|create_sign_request|get_sign_request_by_token|record_email_open`, `insert_job_document|move_photo_to_room|upsert_document_template`, `get_document_templates`, `get_sign_document_templates`); bucket names `job-files|message-attachments`; column probes `esign_status|encircle_media_id|link_clicked_at|email_opened_at|email_open_count`, `file_size|sign_request_id`.
- Catalog checks: fndefs-*.sql awk pass mapping every function body that mentions each table; policies.json filtered to my tables + schema='storage'; triggers.json (all 8 found); views.json (zero references); publications.json (none of mine in supabase_realtime); prod-cron.json (no direct docs jobs; jobid 10 reaches sign_requests via get_real_job_evidence_mismatches); fks.json / indexes.json / grants.json / rls-flags.json filtered; prod-stats.json for all 4 tables; prod-fn-stats.json empty (track_functions off — no per-function call counts available).
- Provenance: `grep -ril` over supabase/migrations/ — no CREATE TABLE for any of the 4 tables (all predate schema-as-code); policy/RPC provenance pinned to 20260701_crm_partner_rls_non_crm_tables.sql, 20260708_dbf_p2_storage_lockdown.sql, 20260708_dbf_p3_anon_policy_closure.sql, 20260708_dbf_p3_sign_document_templates_rpc.sql (+ bridge/drop pair), 20260627_real_job_classification.sql, 20260721_crm_pipeline_auto_advance.sql, 20260726200000_anon_closure_tranche_a.sql (explicitly does-NOT-touch my tables), 20260727143000_anon_closure_tranche_b.sql.
- Verified in source (read, not assumed): submit-esign.js (fallback RPC + worker-side status/expiry gates + service-role client), SignPage.jsx (client-side gates + token-scoped template fetch), track-open.js (public pixel → service-role RPC), send-esign/resend-esign/demo-sheet-pdf/generate-water-loss-report (requireUser), google-drive-import (employee check line 43), storage lockdown migration full header + DDL.
- Ambiguous / could not fully verify: (a) generic timestamp columns marked *uncertain* (`created_at` on document_templates/sign_requests, `updated_at` on job_documents) — plausibly carried by `select=*` payloads, no positive read evidence either way; (b) per-column liveness in production data (e.g., whether old rows carry encircle_media_id values) is unknowable from code + table-level stats — the dead claims rest on the zero-reference trail, not on data absence; (c) prod-fn-stats was empty, so no call-count corroboration exists for any RPC; (d) the effective-anon conclusion on job_documents (#1) is derived from policy text + `is_crm_partner` source + grants — it matches the catalog exactly but was not probed against the live API (read-only discipline).

---

## Amendment — non-app-code blind-spot sweep (2026-07-29)

Every dead claim above was re-checked against the four surfaces application-code search cannot see: (1) repo-root and `docs/` runbooks **executed** against production, (2) recent data-repair migrations that are writes rather than DDL, (3) `UPR-Web-Context.md` annotations naming external consumers this repository cannot inspect, (4) doc-designated computation contracts whose callers are unobservable because `track_functions` is off in production.

**Result: 1 reclassified, 11 survived as dead.**

### Reclassified — no longer dead

- **column `job_documents.encircle_media_id`** → uncertain — dormant contract (doc-designated import-identity guard, unobservable owner-run caller) *(surface 4)*
  - docs/db-foundation-p4-orphan-report.md:20-23 — a LIVE (non-archived) P4 governance report, 'Generated 2026-07-08 against the live shared Supabase (glsmljpabrwonfiltiqm), verified row-by-row against QuickBooks Online and Encircle via the UPR MCP', explicitly classifies job_documents.encircle_media_id as a 1:1 import-identity column that 'already carries a UNIQUE index' — i.e. it names the column as THE double-import guard for an Encircle media record. It is listed in the same breath as five demonstrably-live import keys (inbound_leads.callrail_id, job_notes.encircle_note_id, rooms.encircle_room_id, messages.twilio_sid, payments.stripe_charge_id), and the report's §1 explicitly contrasts these per-row identity columns against 'foreign catalog/parent references' that are correctly NOT uniqueness candidates — so the classification is deliberate, not incidental. The same framing is restated in supabase/migrations/20260708_dbf_p4_external_id_unique_clean.sql:19-22 (comment only, no write). CORROBORATING (also surface 4): docs/database/how-the-data-model-works.md:65 — the canonical database-layer orientation doc — names 'Encircle sync' as a PRIMARY WRITER of job_documents rows in its 'Who Writes What' table ('Documents/Photos | Tech uploads (insert_job_document RPC), Encircle sync, e-sign workers'), and :61 lists job_documents as written on the Claims->Jobs Encircle-sync path. The business process demonstrably still happens and its caller is structurally unobservable: Encircle->UPR import is an owner-run MANUAL procedure via MCP, per UPR-QBO-ENCIRCLE-RECONCILIATION-GUIDE.md §7 'Import gotchas (hit during Tanra Hill)' (:171-177), §8 ':186' encircle_list_media usage, and §8's explicit write instruction 'Writes: use Supabase execute_sql (raw SQL)' (:184) — writes performed through execute_sql / upr_insert / upr_upsert never appear in src/, functions/, scripts/ or upr-mcp/, which is exactly why the code-only pass returned zero. No per-column production stats exist and prod-fn-stats.json is empty (track_functions OFF), so nothing can corroborate or refute actual writes. COUNTER-EVIDENCE, stated honestly: no Encircle worker touches job_documents in code; how-the-data-model-works.md self-subordinates in its own header ('If this guide and UPR-Web-Context.md ever disagree, UPR-Web-Context.md wins'); and the mapper's open question #2 asks the owner whether the integration is still planned. One owner answer resolves this — do NOT kill it unasked.

### Kill-notes — dependencies a future DROP must carry (Phase P5)

- **document_requests (table) — HARD BLOCKER, breaks a live admin feature**
  - merge_jobs() writes this table. Catalog fndefs-q3-06.sql:442 (inside CREATE OR REPLACE FUNCTION public.merge_jobs(p_keep_id uuid, p_merge_id uuid) declared at :348) contains: `UPDATE document_requests SET job_id = p_keep_id WHERE job_id = p_merge_id;`. merge_jobs is LIVE — src/components/MergeModal.jsx:62 `const RPC_MAP = { contact: 'merge_contacts', claim: 'merge_claims', job: 'merge_jobs' };`. plpgsql resolves the relation at execution, so a DROP TABLE without a paired `CREATE OR REPLACE FUNCTION public.merge_jobs(...)` (with that line removed) IN THE SAME MIGRATION makes every job merge throw `relation "document_requests" does not exist`. Independently corroborated by docs/audit/2026-07/evidence/mobile-readiness-r0-recapture-2026-07-25.md:168, which enumerates document_requests among the tables merge_jobs repoints. Verified NOT affected: merge_contacts (fndefs-q3-06.sql:269) and merge_claims contain zero references to document_requests — merge_jobs is the only one.
- **document_requests.job_id (column)**
  - Same blocker as the table, at column granularity — job_id is the exact column merge_jobs UPDATEs. Any column-level drop of job_id (rather than a whole-table drop) breaks merge_jobs identically. It is also the FK column named in the runbook FK map below.
- **document_requests (table) — executed-runbook doc staleness**
  - UPR-QBO-ENCIRCLE-RECONCILIATION-GUIDE.md:186 ('Deletes & FKs', §8) is part of a procedure the owner EXECUTES against production via Supabase execute_sql, and it lists document_requests among the NO ACTION job-child FKs that 'block a job delete' (alongside system_events, payments, conversations, notification_queue, sub_confirmations, vendor_invoices). Dropping the table is safe and in fact REMOVES a blocker, but the runbook's FK map must be corrected in the same change or the next operator follows a stale delete-ordering reference. Structurally confirmed by catalog/fks.json: document_requests_job_id_fkey = `FOREIGN KEY (job_id) REFERENCES jobs(id)` with no ON DELETE clause (= NO ACTION).
- **policy document_requests:allow_authenticated_document_requests**
  - No independent blocker — it is auto-dropped with its table. Sequencing trap: do NOT emit a separate `DROP POLICY` step ordered AFTER the `DROP TABLE` (it errors on a missing relation). Per database-standard.md §6 + §3, the drop is a destructive change requiring a `-- destructive-approved:` marker (CI-enforced by scripts/check-migration-hygiene.mjs) and a paired rollback in supabase/rollbacks/ that recreates the table AND this policy together, plus the two FKs and the pkey.
- **job_documents.encircle_media_id — only if the owner overrides the reclassify above**
  - Two dependencies. (1) The partial UNIQUE index job_documents_encircle_media_id_idx is dropped implicitly with the column — no separate step needed, but it is the exact guard that docs/db-foundation-p4-orphan-report.md:20-23 asserts exists, so that report's §1 must be corrected in the same change or P4's evidence base becomes false. (2) job_documents is a hot table (72,607 seq scans, last scanned 2026-07-29) with heavy live traffic; per database-standard.md §5 a strong-lock DDL on it must not overlap another apply window on the same table.
- **docs/generated/schema-overview.md:55 — regeneration, not a blocker**
  - The generated schema overview lists `| document_requests | 8 | yes | 1 | no |`. It is produced by scripts/db-docs-gen.mjs:124 and must be REGENERATED after any drop, never hand-edited (AGENTS.md: 'Regenerate docs/generated/; never hand-edit a generated report').

### Sweep coverage notes

SWEEP RESULT: 12 dead claims re-checked, 1 reclassified, 11 survived clean. That is well below the billing domain's ~50% rate, and it is a legitimate outcome here — see the coverage audit below before discounting it. The structural reason is that this domain's dead set is almost entirely ONE never-shipped table plus two half-shipped feature columns, whereas billing's dead set sat inside the money surfaces that the owner's manual MCP runbooks actually operate on.

SURFACE-BY-SURFACE COVERAGE (re-verified per the calibration instruction):

Surface 1 (executed runbooks) — Grepped all 25 repo-root *.md and all 198 docs/**/*.md. Extracted every `INSERT INTO <table>` / `UPDATE <table> SET` in the four executed runbooks (UPR-QBO-ENCIRCLE-RECONCILIATION-GUIDE.md, Q2-2026-RECONCILIATION-PLAN.md, Q2-RECON-TASK.md, RECONCILIATION-HANDOFF.md): 9 prescribed writes total, ALL against invoices / claims / jobs. Zero prescribe a write to any Documents & Storage object. The single document_requests hit (recon guide :186) is a prose FK-blocker note inside a delete recipe, not a write — see kill_notes. I explicitly CONSIDERED and REJECTED reclassifying document_requests on it: the surface argues the drop is safe and beneficial (one fewer NO ACTION blocker), not that the table is reachable, and prod stats independently show ins 0 / upd 0 / del 0. Flagging that reasoning so the synthesizer can second-guess it. Residual uncertainty worth one line to the owner: §8 is titled 'gotchas', so it is possible the owner once actually HIT a document_requests block (implying rows existed pre-stats-reset) rather than deriving the list from schema inspection; the surrounding bullets read as schema-derived, so I judged the latter.

Surface 2 (2026-06+ repair/backfill migrations that are really WRITES) — COMPLETELY EMPTY for this domain. Machine-swept every supabase/migrations/*.sql dated 202606*/202607* for document_requests|encircle_media_id|link_clicked_at: exactly ONE hit, and it is a prose comment (20260708_dbf_p4_external_id_unique_clean.sql:20). That migration's actual DDL creates unique indexes on forms.encircle_note_id and google_calendar_links.google_event_id only — it never touches my objects. No dorothy-killian-style data repair reaches this domain.

Surface 3 (external consumers) — UPR-Web-Context.md:635 annotates document_requests as the bare string 'Document request records', with no external app, partner, portal, spreadsheet, Zapier/Make, or owner-automation mention; the surrounding Documents & Esign block (:624-654) annotates sign_requests' columns (token, status, open tracking, the four consent columns) and never mentions link_clicked_at at all. docs/integrations.md contains zero Encircle-media / document-request content (all 'media' hits are CallRail/Twilio MMS). BILLING-CONTEXT.md and ENCIRCLE_API_REFERENCE.md: zero hits for any of my objects. No vendor_invoices-style 'also used by <external app>' annotation exists anywhere in this domain.

Surface 4 (doc-designated mechanisms with unobservable callers) — one hit, the reclassify. Explicitly checked and found NOTHING for the other claims: no doc anywhere designates document_requests as the mechanism for any process (the only prose is the schema-v2 map calling it never-shipped), and no doc designates e-sign click tracking at all.

sign_requests.link_clicked_at IS THE CLEANEST KILL IN THIS DOMAIN — recommend it with confidence. It survived all four surfaces with zero ambiguity and has NO dependency of any kind: not in the 5 rv_* views, not in supabase/tests/ or tests/qa/, not in .github/workflows, not in any of prod-cron.json's 14 jobs, not in scripts/*.sql, not in merge_jobs/merge_contacts/merge_claims, no index, no FK. Worth knowing: the ONLY `clicked_at` anywhere in the repo is `messages.clicked_at` (msg domain, RCS button taps, functions/api/twilio-status.js:134 + 20260709_sms_f01_drift_capture.sql:108) — a different table in a different domain. Do not let a synthesizer conflate them; the msg one is live, this one is not.

NON-BLOCKERS AFFIRMATIVELY VERIFIED CLEAN (so P5 does not re-investigate): all 5 rv_* views (rv_invoices, rv_leads, rv_jobs, rv_payments, rv_time_entries) reference none of job_documents / sign_requests / document_requests / encircle_media_id / link_clicked_at — this domain has no view-projection blocker of the payments.is_depreciation_release kind. Zero INBOUND FKs to document_requests (catalog/fks.json: only two OUTBOUND, job_id -> jobs(id) and requested_by -> employees(id)), so no other table structurally depends on it. Zero test-executed objects: unlike billing's save_estimate_lines, nothing in supabase/tests/ or tests/qa/ names any of my dead objects, so no drop reddens the CI db lane.

DOMAIN-WIDE CAVEAT (deliberately NOT applied per-object, flagging for the synthesizer's judgment): upr-mcp/src/codeIndex.js:6223 carries a search-token metadata entry for document_requests ({name, domain: 'Documents & Esign', tokens: [...]}) that makes the table discoverable by the owner's `upr_search` MCP tool, and the owner's generic `upr_select` / `upr_insert` / `upr_upsert` / Supabase `execute_sql` tools can reach ANY table by name without leaving a code trace. That arbitrary-table reachability is real but universal — it would make every object in the whole audit 'uncertain', so I did not treat it as a per-object surface. It is, however, the mechanism that makes the encircle_media_id reclassify plausible, and it is the reason no code-only pass can ever fully clear a column in this repository.

RECOMMENDED SINGLE OWNER QUESTION that resolves the one reclassify: the mapper's existing open question #2 already asks it — 'should Encircle claim photos ever import into a job's Files tab (that's what job_documents.encircle_media_id was for)?' If the owner says no, encircle_media_id drops to confirmed-dead and the P4 orphan report §1 line 21 should be corrected at the same time.
