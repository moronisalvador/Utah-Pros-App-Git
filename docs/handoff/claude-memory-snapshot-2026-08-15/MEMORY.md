- [iOS Simulator panel Metal crash](ios-sim-panel-metal-crash.md) — the Claude sim panel can't stream on this Mac (macOS 27 beta Metal bug); use `xcrun simctl` for screenshots
- [Driving the iOS Simulator for UI verification](driving-ios-simulator-for-ui-verification.md) — computer-use is the working path when the sim MCP is dead; three traps (accent picker on `type`, wheel scroll never reaches WKWebView, calibrate window scale from two landmarks) that each look like a broken app
- [Sign-out revival guard landed on dev 2026-07-30](signout-defect2-parallel-fixes.md) — plus the .env.local unit-lane env leak: main-checkout and worktree test runs differ on import.meta.env
- [2026-07-30 push outage: dev-hosted outbox + Preview APNS_TOPIC](push-outage-2026-07-30-topic-mismatch.md) — worker repointed to production via integration_config (live config, not in repo); /api/send-push on both origins is the diagnostic
- [gcal ACL finding already resolved](gcal-acl-finding-already-resolved.md) — the notify_google_calendar_sync authenticated-revoke is folded into pending 20260730214500; md5 9c12900f… is a stale draft fingerprint, don't author a duplicate migration
- [Uncaptured local work on the Mac (2026-07-31)](uncaptured-local-work-2026-07-31.md) — an orphaned 11-commit Twilio feature, 3 stashes, 4 local-only branches: do NOT run git gc/prune/stash-clear until pushed
- [git skip-worktree hides files here](git-skip-worktree-hides-files.md) — `git status` clean is not proof; check `git ls-files -v | grep -E '^[a-zS]'` before trusting a clean tree
- [Rescue branches from the 2026-08-01 reconcile](rescue-branches-2026-08-01.md) — never delete either; both verified pushed to origin as of 2026-08-04
- [WIP triage 2026-08-04 verdicts](wip-triage-2026-08-04-verdicts.md) — 45 branches provably dead, 4 worktrees hold real uncommitted work (QBO receive-payment repair!), stashes untriaged
- [S1h personal-ownership migration is pending AND drifted](s1h-personal-ownership-pending-and-drifted.md) — never applied, its preflight would now fail, and the anon policy it closes is still live
- [QBO payment sync — repaired 2026-08-06](qbo-payment-sync-single-point-of-failure.md) — 8-RPC repair applied (ledger 20260806034004), poller rebuilt and PROVEN (first honest sweep 05:17Z); 3 diagnostic traps still apply (LastModifiedByRef, never truncate description, SUM per qbo_invoice_id); Wagner 5993 RESOLVED; **Intuit webhook delivery RESUMED — delete-probe Payment events arrived + processed 17:07 UTC 2026-08-06, UPR self-cleaned**; OPEN: qa-staging apply of the 8-RPC repair (byte-exact payload)
- [Conversation push tap three-layer break — all fixed 2026-08-06](conversation-push-tap-three-layer-break.md) — server lockout fixed, lease purge fixed, cold-start hold shipped (PR #587 + TestFlight build); techs must update the app; security posture: push policy at dispatch, 5-min TTL, binding re-check at readiness
- Receive-payment feature COMPLETE + PROMOTED TO MAIN 2026-08-06 (PRs #589/#591 + same-day redesign PR #593, merged 21:09Z as 18977342 during the GitHub Actions major outage — CI landed via close/reopen retriggers at ~15% webhook throttle; full local gauntlet on pristine worktree was the backstop proof): QBO-style form (searchable combobox — contract-pinned; running Amount-received total; check-ring tap-to-fill; inline invoice loads), entries = + New menu, Collections, InvoiceEditor, native More, tech Dash + FAB pill (violet, billing-gated, WAAPI menu motion); worker GET mirror-only (balance_due, zero QBO reads)/failure-telemetry; ops-health payment heartbeat; db-lane serialized; iOS release dispatch needs -f publish_to_testflight=true or the upload step SKIPS; provenance freshness gate = 6h, re-stamp via capture script --print-sql → MCP read-only → --assemble (never hand-edit); **TestFlight void bug FIXED 2026-08-06**: production fastlane variant had internal_group nil → uploads processed VALID but never reached "UPR Technicians" (auto-distribute OFF) — group now named on dev (needs dev→main to affect releases), stranded builds retro-assignable via "iOS ASC distribute" workflow (194.1 delivered + email-proven 21:36Z), "iOS ASC diagnose" reads groups/build states + team users/invitations via ios-testflight environment secrets; "iOS ASC reinvite" re-sends expired ASC invites (retries the post-delete 409; deliberate-create needs explicit roles) — Ben Palmieri re-invited 2026-08-06 as DEVELOPER at benjapalmieri@gmail.com (NOT ben@utah-pros.com), **after he accepts he still needs adding to the TestFlight groups**; Matheus (Matheusaegm5@gmail.com) invite also expired, not re-sent; dev-app upload budget raised to 45-min ceiling same as official (both pipelines died 124 mid-processing-wait before); **Ben ACCEPTED + enrolled in "UPR Technicians" 2026-08-06 23:23Z** via new "iOS ASC add tester" workflow (endpoint that works: POST /v1/betaTesters with betaGroups relationship + 409→relationship-link fallback; bulkBetaTesterAssignments paths are 404 on this API); conversations media repair in shared MessageBubble (fixed 220x200 boxes + in-page tech Lightbox with Esc/arrows, fileUrl passes absolute URLs) covers web + tech v2 in one place — PROMOTED via PR #595 (merge f6a3b44a, 2026-08-07 00:03Z), production media fix verified live 00:05Z; SETTLED 2026-08-07: official 195.1 then 196.1 (lightbox safe-area ✕, main@d01d19eb via PR #596) both auto-delivered; UPR Dev progression 99.1 (JS gestures) → 100.1 (pinch-feel fix) → **102.1 = the Swift NativePhotoViewer (UIScrollView physics), owner-validated "zoom and drag feels perfect"** — pattern + backlog in [[swift-native-plugin-pattern]], learnings codified in motion-standard.md (resting-transform trap = HARD checker failure); provenance re-stamp recipe (committed-file-derived --assemble after zero-drift MCP comparison) used successfully twice 2026-08-06
- [Direct iPhone install via Xcode CLI](direct-iphone-install-workflow.md) — UPR Dev scheme + devicectl to the owner's phone; sandbox push tokens still served (across-environments sender); phone-locked launch errors are harmless; owner keeps a logged-in sim session for agent-driven UI verification
- [Mobile UX design doctrine (owner-directed)](mobile-ux-design-doctrine.md) — POS-style step wizards via impeccable for ALL mobile surfaces, desktop forms stay on desks; reference: ReceivePaymentMobileFlow; fixed-not-sticky bottom bars over --tech-nav-height
- [Full handoff: 2026-07-31 Mac reconcile + loss audit](../HANDOFF-2026-07-31-mac-reconcile.md) — complete state, pending migrations, ordered recovery plan
- [Official-app TestFlight freeze (2026-08-07)](testflight-release-policy.md) — dispatch ONLY UPR Dev builds; official app frozen at 196.1 until the owner authorizes the next cut
- [Swift native plugin pattern + backlog](swift-native-plugin-pattern.md) — the proven app-local plugin recipe (pbxproj wiring, .overFullScreen trap, sim verification); share sheet + QuickLook SHIPPED 2026-08-07, remaining: bg uploads → Live Activity
- [Job Hub redesign — starting point](job-hub-redesign-starting-point.md) — NEXT BIG PROJECT: the Apple Field Pro design is locked in `docs/tech-redesign/` (not lost); the trap is blind-merging the 697-behind prototype branch, and there's one scoping question to settle before any code
- [Stale sim bundle looks like a code bug](job-hub-stale-sim-bundle-trap.md) — grep the INSTALLED app before debugging; plus the simctl deep-link recipe for driving the sim without the (dead) panel
- [Job Hub wave 2 — what's LEFT + 3 corrections](job-hub-wave2-remaining.md) — plan is `docs/job-hub-wave2-roadmap.md`; Docs/Notes were already built, the below-fold is re-housing, and the top item is the missing `/tech/appointment/:id` redirect (push links from months ago land on the legacy page)
- [Job Hub wave 2 spec — owner-approved](job-hub-wave2-spec.md) — the published artifact IS the spec; carries the no-ticking-clock ruling, the three-circle control, Message/Docs/Notes/More, and Docs-nouns-vs-More-verbs
- [Field Pro = the native-Swift rewrite target](field-pro-is-the-native-rewrite-target.md) — owner ruling: today's web Job Hub copies Field Pro's CONTENT into UPR's existing design on purpose; Field Pro's own look lands only with a future all-native rewrite
- [Clock-tap loss root cause (fixed 2026-08-07)](clock-tap-loss-root-cause.md) — GPS gated the payroll write + a self-disarming Finish confirm; and three measured facts that contradict the obvious guesses (location IS live at 88% capture, the DB is clean, offline was never the silent path)
- [Worktree npm install rewrites the lockfile](worktree-npm-install-lockfile-trap.md) — `npm test` needs `npm install` in a worktree, which silently strips `libc` fields from package-lock; revert it before staging
- [Harvested worktrees look abandoned but are merged](harvested-worktree-orphans.md) — uncommitted ≠ unmerged; check BOTH directions, a stale tree can revert a shipped fix
- [Close-out ends in PR + notification (owner-directed 2026-08-08)](close-out-ends-in-pr-and-notification.md) — never end a session on a finished uncommitted diff; PR into dev + notify, merge stays owner-gated
- [A running Vite dev server fails unrelated unit tests](vite-devserver-poisons-test-lane.md) — worktrees symlinking node_modules share the .vite cache; stop the server before believing a red run
- [Status tokens fail AA on their own tint](status-token-contrast-on-own-tint.md) — all four families, both directions; use --text-primary on tints and let the border/icon carry the hue
- [Shared clone: a moving base poisons diff-scoped gates](shared-clone-moving-base-poisons-diff-gates.md) — another session's fetch advances your origin/dev mid-session; findings on files you did not touch mean your base moved, not shared debt
- [Two tools need OPPOSITE git roots](wip-open-writes-outside-worktree.md) — wip register wants the worktree, session ledger wants the main checkout; never unify the root helpers (bug fixed PR #614). Also: worktrees:clean can delete a worktree a session is live in
- [Shared checkout: the index and HEAD betray your commit](shared-checkout-index-and-head-betray-commits.md) — `git commit` takes another session's staged files ("explicit path" does NOT help; use `commit -- path`), and HEAD may be on someone else's branch — `0 0` sync proves nothing
- [TestFlight distribute lane fails; the ASC UI works](testflight-distribute-lane-fails-ui-works.md) — `add_beta_groups` 404s on this tenant even with the correct build id; assign via App Store Connect's GROUPS **+**, and never trust diagnose's `groups=[]`
- [Photo capture flow doctrine](photo-capture-flow-doctrine.md) — camera opens instantly, album is an icon in/beside the camera, never a choose-first prompt (owner ruling 2026-08-14); owner amended the COMPOSER attach flow same day: a native-look menu may precede it (Take Photo → custom camera, Photo Library, Templates, Internal note)
- [Sim app-local plugin availability flake](sim-app-local-plugin-availability-flake.md) — isPluginAvailable intermittently false on the beta-macOS sim (reinstall fixes), Device Hub window vanishes, and sibling sessions collide on the one booted sim — all three mimic app bugs
- [Worktree cap-sync + sim-login traps](worktree-capsync-and-sim-login-traps.md) — cap sync rewrites Package.swift through the node_modules symlink (revert before staging); native builds have NO dev-login button, so a dead sim session makes signed-in UI verification owner-gated
- [Review instruction is a hypothesis](review-instruction-is-a-hypothesis.md) — trace the blast radius before applying a reviewer's proposed fix; twice on PR #644 the literal instruction would have regressed production
- [Claude runs the session fleet](agent-runs-the-session-fleet.md) — owner-directed 2026-08-14: coordinate/merge/archive all sessions; escalate only promotion, migration apply, provider/flag, dispatch, money, rules edits
- [Absolute Edit paths escape the worktree](edit-absolute-path-escapes-worktree.md) — a worktree session's absolute path lands in the shared main checkout; the worktree's `git status` stays clean, so check both
