/**
 * ════════════════════════════════════════════════
 * FILE: nativeHaptics.js
 * ════════════════════════════════════════════════
 *
 * WHAT THIS DOES (plain language):
 *   Replaces phone feedback with harmless empty actions while the local browser
 *   check renders the real calculator.
 *
 * DEPENDS ON:
 *   Packages:  none
 *   Internal:  none
 *   Data:      reads  → none
 *              writes → none
 * ════════════════════════════════════════════════
 */
export function impact() {}
export function notify() {}
export function selection() {}
